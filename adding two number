#include <stdio.h>
int main()
{
    int a,b,sum;
    printf("Enter two number:");
    scanf("%d%d",&a,&b);
    sum = a+b;
    printf("sum = %d",sum);
    return 0;
}

modification 
find diffference
find product
find quotient
accept three numbers instead of two
find average
